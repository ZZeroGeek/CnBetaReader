<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimaryDark">@color/statusBar</item> <!--状态栏颜色-->
        <item name="colorPrimary">@color/statusBar</item>  <!--ActionBar颜色-->
        <item name="android:textColorPrimary">@color/white</item> <!--ActionBar字体颜色-->
        <!-- Customize your theme here. -->
    </style>

</resources>
